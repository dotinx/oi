#include <bits/stdc++.h>
using namespace std;
//(a + c) * b = a * b + c * b
//((a + c) * b + e) *d = a * b * d + b * c * d + e * d
typedef long long int ll;
ll datas[200000];
struct func{
  int operator;
  int object;
  ll value;
};
