#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
ll n,m,ans;
int main(){
  scanf("%lld%lld",&n,&m);
  for(int i = 0;i < n - 1;i++){
    ll u,v;
    scanf("%lld%lld",&u,&v);
  }
  for(int i = 0;i < n;i++){
    ll w;
    scanf("%lld",&w);
  }
  for(int i = 0;i < m;i++){
    ll s,t;
    scanf("%lld%lld",&s,&t);
  }
  for(int i = 0;i <= n;i++)
  return 0;
}
